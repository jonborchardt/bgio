# 14.9 — README demo claim accuracy

**Parent:** [14-playtest-followups.md](14-playtest-followups.md)

## Goal

[../README.md](../README.md) (post-13.4) advertises the GitHub Pages
URL as "Demo (no save, no login)". The 2026-04-29 playtest found
that the demo as it currently stands has no playable action surface
(see the master notes file). Either land 14.1 + 14.2 first and
update the README to confirm the demo works, or downgrade the claim
to "preview" and point users at `npm run dev:full` for an
actually-playable session — issue O1.

## Files

- Modify [../README.md](../README.md).

## Steps

1. If 14.1 + 14.2 have landed at the time this sub-plan is worked:
   - Update the demo line to read "Demo at the GitHub Pages URL —
     hot-seat, single-tab. Pick a seat from the tab strip and play
     all four roles from one browser. No save, no login."
2. Else:
   - Downgrade to "Preview at the GitHub Pages URL — work-in-
     progress, not yet playable end-to-end. Run locally with
     `npm run dev:full` for the full experience."
3. Either way, the README references this sub-plan's status so a
   future visitor can find the rationale.

## Tests

- None. Verify by skimming the diff.

## Dependencies

13.4 (the existing demo line), 14.1 + 14.2 (the unblockers).

## Out of scope

- Screenshots in the README. Add when the UI is stable.
