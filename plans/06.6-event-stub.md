# 06.6 — domesticPlayGreenEvent (stub)

**Parent:** [06-domestic.md](06-domestic.md)

## Goal

Mirror 04.4 / 05.4 for the green event color owned by Domestic.

## Files

- Create `src/game/roles/domestic/playGreenEvent.ts`.
- Register in `src/game/moves.ts`.
- Create `tests/roles/domestic/playGreenEvent.test.ts`.

## API

```ts
export const domesticPlayGreenEvent: Move<SettlementState>;
// args: cardID: string
```

## Steps

Identical structure to 05.4 / 04.4 — validate caller, validate event hand,
dispatch via 08.2, mark used. Until 08 lands, short-circuit `INVALID_MOVE`
when `G.events` is undefined.

## Tests

- Without 08: `INVALID_MOVE`.
- With stub: card consumed, dispatcher called.
- Two greens in one round: second is `INVALID_MOVE`.
- Wrong color: `INVALID_MOVE`.

## Dependencies

01.1, 02.1, 02.2.

## Out of scope

- Effects.
