# 07.6 — foreignPlayRedEvent (stub)

**Parent:** [07-foreign.md](07-foreign.md)

## Goal

Mirror 04.4 / 05.4 / 06.6 for the red event color owned by Foreign.

## Files

- Create `src/game/roles/foreign/playRedEvent.ts`.
- Register in `src/game/moves.ts`.
- Create `tests/roles/foreign/playRedEvent.test.ts`.

## API

```ts
export const foreignPlayRedEvent: Move<SettlementState>;
// args: cardID: string
```

## Steps

Identical structure to 04.4 / 05.4 / 06.6 — validate caller, validate
event hand, dispatch via 08.2, mark used. Until 08 lands, short-circuit
`INVALID_MOVE`.

## Tests

- Without 08: `INVALID_MOVE`.
- With stub: card consumed, dispatcher called.
- Two reds in one round: second is `INVALID_MOVE`.
- Wrong color: `INVALID_MOVE`.

## Dependencies

01.1, 02.1, 02.2.

## Out of scope

- Effects.
