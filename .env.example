VITE_CLIENT_MODE=hotseat
VITE_SERVER_URL=http://localhost:8000

# 12.7 — `npm run dev:full` knobs.
# Switch the client to networked mode when running both processes.
# VITE_CLIENT_MODE=networked
PORT=8000
STORAGE_KIND=sqlite
SQLITE_PATH=./.dev-data/settlement.sqlite
